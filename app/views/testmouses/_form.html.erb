<%= form_with(model: testmouse, local: true) do |form| %>
  <% if testmouse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testmouse.errors.count, "error") %> prohibited this testmouse from being saved:</h2>

      <ul>
        <% testmouse.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
